# Product

## Users

主要用户是需要查找公开单细胞与空间组学数据集的中文研究者、学生和初次使用者。他们可能不了解各数据门户的字段差异，也不应被要求先掌握检索语法、向量模型或 LLM 术语。熟练用户仍需要可复现的筛选条件、明确的数据来源和不会被教程阻塞的高效入口。

**受众不变量（复用者，不是新颖性判官）**：用户来这里是为了**找到可复用的公开数据集**，不是为了判断某个研究方向有没有人做过。语料是数据集**库存**、不是**文献库**——「本目录内无匹配」只说明本目录收录的来源里没有，**推不出**「这个研究没人做过 / 这是新颖方向」（scRNA-seq 数据多在我们不索引的 GEO/SRA）。产品文案一律走**供给侧**措辞（「有哪些可复用数据集」「本目录内无匹配」），永不出现新颖性/独占性断言。该不变量由 `tests/test_audience_copy_invariants.py` 机械钉死。

## Product Purpose

BioData Agent 把中文实验需求转换为可审计的结构化约束，在多个公开数据集目录中完成硬过滤、排序、结果解释和下载入口整理。成功意味着用户能快速完成第一次有效查询，理解系统识别了哪些条件、使用了哪些检索手段，并能在需要时安全开启本地语义排序或 AI 能力，同时始终保持硬约束不被排序层突破。

## Brand Personality

准确、克制、坦诚。界面应像一位熟悉数据目录但不会替用户夸大结论的研究助理：先给可验证结果，再用普通语言解释自动解析、排序和回退行为。

## Anti-references

- 不做充满模型名、内部字段和晦涩缩写的工程控制台。
- 不把 AI 包装成万能答案，也不隐藏联网、费用、非确定性或回退情况。
- 不用长篇强制教程挡住首次查询，不重复弹出已跳过或已完成的引导。
- 不为增加“科技感”引入霓虹、玻璃拟态、装饰性渐变文字或无意义动效。

## Design Principles

1. 先到达价值：新手教程围绕完成一次真实查询，而不是罗列全部功能。
2. 自动但不神秘：自动解析和自动排序必须同时给出简洁、可展开的执行解释。
3. 正确性高于惊喜：硬过滤、终检和安全弃权优先于任何语义或 AI 排序收益。
4. 渐进披露：默认路径保持简单，模型、候选池和诊断信息在设置或展开项中按需出现。
5. 专家可跳过：教程可跳过、可重放；手动策略始终可用，自动策略不得抹去实际后端状态。

## Accessibility & Inclusion

延续现有键盘可操作、语义化标签、可见焦点、移动端适配和 `prefers-reduced-motion` 降级。首次引导必须可跳过、可关闭、可通过帮助页重放，不依赖颜色单独表达状态，也不锁死页面的核心操作。
