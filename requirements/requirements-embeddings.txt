# Optional dependencies for local semantic reranking.
# Install them with the same Python environment used to run BioData Agent:
#
#   <python> -m pip install -r requirements-embeddings.txt
#   <python> scripts/fetch_embedding_model.py
#
# Use --all with fetch_embedding_model.py to download every supported local model.
# Model weights are not bundled with the project. If dependencies or weights are missing,
# the application falls back to rule-based ordering without blocking the main workflow.

sentence-transformers>=2.2,<4
# Preferred download source; the fetch script can fall back to Hugging Face.
# 下限与安装版 model-win-x64.in 的精确钉版（modelscope==1.39.1）对齐。
modelscope>=1.39.1
